# Premium API

## 本番本線（推奨）

**コスタバと同じサーバー:** [`gcq-api/`](./gcq-api/)  
→ https://costuba.online/gcq-api/

設置手順は **[gcq-api/README.md](./gcq-api/README.md)**。

## ローカル試作（任意）

Node メモリ DB: `npm run dev`（このディレクトリ）  
Cloudflare Workers + D1: `wrangler`（試作用・本番はコスタバ PHP を使う）
