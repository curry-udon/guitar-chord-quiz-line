{
  "name": "guitar-chord-quiz-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node ./scripts/dev-server.mjs",
    "cf:dev": "wrangler dev",
    "cf:deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute gcq-premium --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute gcq-premium --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
